version: '3.8'
services:
  qdrant:
    image: qdrant/qdrant:latest
    ports:
      # Bind to loopback only — local dev / reviewer demo, not a public service.
      - "127.0.0.1:6333:6333"
      - "127.0.0.1:6334:6334"
    volumes:
      - ./qdrant_storage:/qdrant/storage
